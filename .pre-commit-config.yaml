repos:
  - repo: local
    hooks:
      - id: swiftlint
        name: SwiftLint (strict)
        entry: swiftlint lint --strict
        language: system
        types: [swift]
        pass_filenames: false
        exclude: ^\.agents/
